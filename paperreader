#!/usr/bin/env python3
"""
PaperReader CLI - Quick Start Entry Point

This is a convenience wrapper for the main CLI.
Usage: ./paperreader [COMMAND] [OPTIONS]

Examples:
    ./paperreader process --paper papers/example.pdf
    ./paperreader stats
    ./paperreader --help
"""

import sys
from pathlib import Path

# Add project root to path
project_root = Path(__file__).parent
sys.path.insert(0, str(project_root))

# Import and run CLI
from cli.main import cli

if __name__ == '__main__':
    cli()
